{
  "name": "youtube-buddy-functions",
  "version": "0.1.0",
  "private": true,
  "description": "YouTube Buddy Pro-Tier cloud endpoints (ADR PRD §7.6): Gemini proxy, EU region",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "@google/genai": "^1.0.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.3.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
